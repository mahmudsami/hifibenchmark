# ── Generated / large data (regenerated by the pipeline) ─────────────────────
# References are downloaded (scripts/01_download_references.sh); reads, truth,
# indexes, and all mapping/eval outputs are produced by the benchmark.
data/
results/

# ── Unpublished manuscript & figure sources (excluded from this repo) ─────────
paper/
method_figures/

# ── Unused legacy asset (simulator is pure-Python; pbsim/QSHMM not used) ──────
*.model

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ── OS / editor / tooling cruft ──────────────────────────────────────────────
.DS_Store
.claude/
*.swp
*~
